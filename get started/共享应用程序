现在我们已经建立了一个镜像，让我们分享它！要共享Docker镜像，您必须使用Dockerregistry。默认的注册表是Docker Hub，是我们使用过的所有镜像的来源。
Create a repo创建一个仓库       repository
要推送镜像，我们首先需要在Docker Hub上创建一个仓库。
1、注册或登录Docker Hub.
2、单击创建存储库按钮。
3、对于仓库名称，请使用getting-started.确保可见性是Public.
4、点击create按钮。
此命令将推送一个新的标志到仓库
docker push docker/getting-started:tagname

推送镜像
1、在命令行中，尝试运行您在Docker Hub上看到的推送命令。请注意，您的命令将使用您的命名空间，而不是“docker”。
 $ docker push docker/getting-started
 The push refers to repository [docker.io/docker/getting-started]
 An image does not exist locally with the tag: docker/getting-started
为什么失败了？推送命令正在寻找一个名为docker/getting-started的镜像，但没有找到一个。如果你跑了docker image ls，你也不会看到的。

要解决这个问题，我们需要“标记”我们构建的现有映像，以便为它命名另一个名称。
2、使用命令登录Docker Hubdocker login -u YOUR-USER-NAME.
3、使用docker tag命令给出getting-started图像一个新的名字。一定要换掉YOUR-USER-NAME使用您的Docker ID。
 docker tag getting-started YOUR-USER-NAME/getting-started
4、现在，请重试您的推送命令。如果要从Docker Hub复制值，则可以删除tagname部分，因为我们没有在镜像名称中添加标记。如果不指定标记，Docker将使用名为latest的标记.
  docker push YOUR-USER-NAME/getting-started
5、在终端中，启动新推送的应用程序。
 docker run -dp 3000:3000 YOUR-USER-NAME/getting-started
 你应该看到镜像被拉下来，最终启动！



